<nav class="navbar navbar-expand-lg py-4" style="background: var(--clr-tertiary);" id="myNavbar">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
  <span></span>
</button>
    <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link mx-5 fs-5" style="color: var(--clr-secondary);" href="/#services">Services</a>
        </li>
        <li class="nav-item">
          <a class="nav-link mx-5 fs-5" style="color: var(--clr-secondary);" href="/#ventes">Ventes véhicules</a>
        </li>
        <li class="nav-item">
          <a class="nav-link mx-5 fs-5" style="color: var(--clr-secondary);" href="/#avis">Avis</a>
        </li>
        <li class="nav-item">
          <a class="nav-link mx-5 fs-5" style="color: var(--clr-secondary);" href="/contact">Contactez-nous</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
<script>
  document.addEventListener('DOMContentLoaded', function() {
    const navbar = document.getElementById('myNavbar');
    const headerHeight = {{ headerHeight | default(60) }};
    const scrollOffset = 50;
    let isNavbarFixed = false;

    window.addEventListener('scroll', function() {
      if (window.scrollY > headerHeight + scrollOffset && !isNavbarFixed) {
        navbar.classList.add('fixed-top', 'navbar-transition');
        isNavbarFixed = true;
      } else if (window.scrollY <= headerHeight + scrollOffset && isNavbarFixed) {
        navbar.classList.remove('fixed-top', 'navbar-transition');
        isNavbarFixed = false;
      }
    });
  });
</script>